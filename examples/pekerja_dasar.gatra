// pekerja_dasar.gatra — Worker thread asli via 'pekerja'/'jalankan' (PRD bagian 21)

pekerja fungsi hitung(data: angka): angka {
  balik data * 2
}

fungsi asinkron utama(): tiada {
  isi hasil = tunggu jalankan hitung(21)
  cetak("Hasil dari pekerja:")
  cetak(hasil)
}

utama()
